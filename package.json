{
  "scripts": {
    "test": "vitest run",
    "test:coverage": "npx vitest run --coverage",
    "test:server": "cd server && npm run test:coverage",
    "test:client": "cd client && npm run test:coverage",
    "test:all": "npm run test:server && npm run test:client",
    "test:coverage:all": "npm run test:server && npm run test:client && node scripts/combineCoverage.js",
    "test:coverage:summary": "npm run test:server && echo '\\n=== CLIENT COVERAGE ===' && npm run test:client"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.6.3",
    "@vitest/coverage-v8": "^1.6.1",
    "jsdom": "^26.1.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "vitest": "^1.6.1"
  }
}
